# ML with torch GPU acceleration (2x faster than Rust CPU when CUDA available, 2-3GB VRAM)
# Rust binary (ml-rust) is 35M, no pip deps, auto-installed, CPU-only, low RAM
# For GPU acceleration, install torch version:
deepfilternet==0.5.6
soundfile==0.13.1
torch==2.6.0
torchaudio==2.6.0
# For CPU-only, use: pip install torch==2.6.0+cpu torchaudio==2.6.0+cpu --extra-index-url https://download.pytorch.org/whl/cpu
